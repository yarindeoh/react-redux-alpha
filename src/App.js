import React from 'react';

import 'resources/scss/style.scss';
import MovieList from 'movieList/MovieList';

export const App = () => {
    return (
        <div>
            <MovieList />
        </div>
    );
};
